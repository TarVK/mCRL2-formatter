/** Latex formatting options */
export type ILatexOptions = {
    /** The number of spaces in the input to consider as a tab */
    inputIndent: number;
    /** The character to use to represent tabs */
    tabCharacter: string;
};
